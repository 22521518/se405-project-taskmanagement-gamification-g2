query GetConversationMembers($conversationId: ID!) {
    getConversationMembers(conversationId: $conversationId) {
        uuid
        displayName
        avatarUrl
        email
        username
        isOnline
    }
}