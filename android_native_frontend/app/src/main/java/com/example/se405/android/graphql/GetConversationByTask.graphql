query GetConversationByTask($taskId: ID!) {
    getConversationByTask(taskId: $taskId) {
        uuid
        type
        name
        taskUuid
        participants {
            uuid
            displayName
            avatarUrl
        }
    }
}