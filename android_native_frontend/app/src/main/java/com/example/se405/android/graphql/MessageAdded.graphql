subscription OnMessageAdded($conversationId: ID!) {
    messageAdded(conversationId: $conversationId) {
        uuid
        content
        type
        fileUrl
        fileName
        fileSize
        createdAt
        sender {
            uuid
            displayName
            avatarUrl
            email
        }
        replyTo {
            uuid
            content
            sender {
                displayName
            }
        }
    }
}